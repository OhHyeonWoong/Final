<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper PUBLIC
"-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member-mapper">

	<resultMap id="MemberResult" type="Member">
		<id property="userId" column="userid"/>
		<result property="userPwd" column="userpwd"/>
		<result property="userName" column="username"/>
		<result property="gender" column="gender"/>
		<result property="age" column="age"/>
		<result property="phone" column="phone"/>
		<result property="email" column="email"/>
		<result property="hobby" column="hobby"/>
		<result property="etc" column="etc"/>
		<result property="enroll" column="enroll_date"/>
		<result property="lastModified" column="lastmodified"/>		
	</resultMap>
	
	<resultMap id="MemberListResult" type="Member">
		<id property="userId" column="userid"/>
		<result property="userPwd" column="userpwd"/>
		<result property="userName" column="username"/>
		<result property="gender" column="gender"/>
		<result property="age" column="age"/>
		<result property="phone" column="phone"/>
		<result property="email" column="email"/>
		<result property="hobby" column="hobby"/>
		<result property="etc" column="etc"/>
		<result property="enroll" column="enroll_date"/>
		<result property="lastModified" column="lastmodified"/>		
	</resultMap>
			
	<insert id="MemberInsert" parameterType="Member" flushCache="true" statementType="PREPARED">		
		insert into member 
		values 
		(#{userId}, 
		#{userPwd}, 
		#{userName}, 
		#{gender}, 
		#{age}, 
		#{phone}, 
		#{email}, 
		#{hobby}, 
		#{etc}, 
		sysdate, 
		sysdate)		
	</insert>
	
	<update id="MemberUpdate" parameterType="Member" flushCache="true" statementType="PREPARED">
		update member 
		set userpwd = #{userPwd}, 
		age = #{age}, 
		phone = #{phone}, 
		email = #{email}, 
		hobby = #{hobby}, 
		etc = #{etc}, 
		lastmodified = sysdate 
		where userid = #{userId}
	</update>
	
	<delete id="MemberDelete" parameterType="Member" flushCache="true" statementType="PREPARED">
		delete from member 
		where userid = #{userId}
	</delete>
	
	<select id="MemberLogin" parameterType="Member" resultType="Member" resultMap="MemberResult">
		
		<![CDATA[
			select * from member 
		]]>	
		
		<!-- <if test="userId != null">
			<![CDATA[
				where userid= #{userId}
			]]>	
		</if>
		
		<if test="userPwd != null">
			<![CDATA[
				and userpwd = #{userPwd}
			]]>
		</if> -->
		
		<choose>
			<when test="userId != null">
				<![CDATA[
					where userid= #{userId}
				]]>	
			</when>
			<when test="userPwd != null">
				<![CDATA[
					and userpwd = #{userPwd}
				]]>
			</when>			
		</choose>
					
	</select>
	
	<select id="MemberCheck" parameterType="int" resultType="Member" resultMap="MemberResult">
		select count(userid) from member 
		where userid = #{userId}
	</select>
	
	<select id="MemberList" parameterType="Member" resultType="Member" resultMap="MemberListResult">
		select * from member
	</select>
		
	<select id="MemberSelect" parameterType="Member" resultType="Member" resultMap="MemberResult">
		select * from member 
		where userid = #{userId}
	</select>	

</mapper>
